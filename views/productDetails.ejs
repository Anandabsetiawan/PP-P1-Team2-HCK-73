<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Categories</title>

    <!-- Script -->
    <%- include('./partials/_script') %>
</head>
<body>
    <!-- Navbar -->
    <%- include('./partials/_navbar') %>

    <!-- Product Details -->
    <div class="container mx-auto">
        <div class="grid grid-cols-2 md:grid-cols-3 gap-4 my-10">
            <div>
                <img src="<%= product.imageURL %>" alt="productImage">
            </div>
            <div>
                <h3 class="mb-7 text-lg font-bold container mx-auto"><%= product.productName %></h3>
                <label class="mb-5 text-base font-bold container mx-auto">Description:</label>
                <p class="mb-5 text-base font-normal container mx-auto text-justify"><%= product.description %></p>
            </div>
            <!-- Buy Card -->
            <div class="p-6 mx-auto max-w-lg text-center text-gray-900 bg-white rounded-lg border border-gray-100 shadow dark:border-gray-600 xl:p-8 dark:bg-gray-800 dark:text-white">
                <h3 class="mb-4 text-2xl font-semibold">Buy This Product</h3>
                <!-- Stock -->
                <label class="block mb-2 text-xl font-medium text-gray-900 dark:text-white">Stock:</label>
                <p class="block mb-2 text-2xl font-bold"><%= product.stock%></p>
                <!--  -->
                <div class="flex justify-center items-baseline my-8">
                    <span class="mr-2 text-xl font-bold">Total Price: </span>
                    <span class="mr-2 text-2xl font-extrabold"><%= rupiahFormat(product.price) %></span>
                </div>

                <!-- Buy Button -->
                <div>
                    <a href="/buyer/buyProduct/<%= product.id%>" class="text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 dark:bg-blue-600 dark:hover:bg-blue-700 focus:outline-none dark:focus:ring-blue-800">Buy</a>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Footer -->
     <%- include('./partials/_footer') %>

</body>
</html>